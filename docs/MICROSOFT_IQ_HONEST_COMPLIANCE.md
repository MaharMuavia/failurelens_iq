# Microsoft IQ Honest Compliance

FailureLens IQ is a DEMO-READY MVP WITH AZURE PRODUCTION ADAPTERS.

OpenAI direct API does not replace Microsoft IQ.

Real Azure calls are enabled only when credentials are provided.

Live Microsoft IQ is claimed only when Azure AI Search returns real grounding references. Demo mode uses a local Foundry IQ-compatible adapter and must not fake live Azure verification.
